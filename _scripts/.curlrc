# .curlrc file

# Follow redirects
--location

# Show verbose output
--verbose

# Include headers in the output
--include

# Set the user agent
--user-agent "Your User Agent String"

# Specify a default content type
--header "Content-Type: application/json"

# Save cookies to a file and send them in subsequent requests
--cookie-jar ~/.curl_cookies
--cookie ~/.curl_cookies

# Disable SSL verification (not recommended for security reasons)
--insecure

# Limit the maximum time the request is allowed to take
--max-time 30

# Resume transfer if interrupted
--continue-at -

# Enable compressed responses
--compressed
